# Dependencies
node_modules

# Build and coverage
dist
coverage

# Environment (do not commit secrets)
.env
.env.local
.env.*.local

# MCP config (may contain GitHub PAT)
.cursor/mcp.json

# Logs and OS
*.log
.DS_Store

# Optional: keep PDF out of repo if large
# /PoC Take Home.pdf
